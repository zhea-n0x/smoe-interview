This folder contain files for dev purpose, e.g. the postman collection for API testing.

# USAGE

## To use the collection, firstly need to setup the environment and add "base_url" variable with value "localhost:8080/api" 